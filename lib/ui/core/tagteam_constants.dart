import 'package:flutter/material.dart';

const Color kLightBackgroundColor = Color(0xFF293C4D);

const List<String> channelSettingsStrings = ['Moderators only', 'Invite only', 'Allow image sending'];

const TextStyle kTeamPageTitle = TextStyle(fontSize: 24.0, color: Colors.white, fontWeight: FontWeight.bold);
